/**
 * Main class to run the TicTacToe game.
 */
public class Main {
    /**
     * Main method to run the TicTacToe game.
     * @param args The command line arguments.
     */
    public static void main(String[] args) {
        TicTacToeController controller = new TicTacToeController();
    }
}